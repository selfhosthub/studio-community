{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/selfhosthub/studio-community/main/schemas/comfyui-workflow.schema.json",
  "version": "1.0.0",
  "title": "Studio ComfyUI Workflow",
  "description": "A ComfyUI workflow catalog package: Studio metadata + the native ComfyUI API graph in a single file. Metadata fields (slug, version, name, description, author, category) live at the top level; the raw ComfyUI graph (numeric node IDs as keys) lives under the `graph` key, opaque to Studio and rendered by the worker after parameter injection.\n\nStability: future versions of this schema must remain backward-compatible with content authored against earlier versions of itself - once a package is published with a given version, it must continue to validate against the schema in force at install time. Add fields conservatively (optional first); promote to required only with a major schema revision and a migration story.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "slug",
    "version",
    "name",
    "description",
    "author",
    "graph"
  ],
  "properties": {
    "$schema": {
      "type": "string"
    },
    "slug": {
      "type": "string",
      "description": "Stable identifier used as the catalog key. Must match the file basename.",
      "pattern": "^[a-z][a-z0-9-]*/[a-z][a-z0-9-]*$"
    },
    "version": {
      "type": "string",
      "description": "Semantic version of the package.",
      "pattern": "^\\d+\\.\\d+\\.\\d+$"
    },
    "name": {
      "type": "string",
      "description": "Human-readable workflow name shown in the UI.",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "description": "Short description of what the workflow does."
    },
    "author": {
      "type": "string",
      "description": "Package author."
    },
    "category": {
      "type": "string",
      "description": "Primary category for grouping in the UI (e.g. image, video, audio)."
    },
    "graph": {
      "type": "object",
      "description": "Native ComfyUI API graph - keys are node IDs (string-encoded integers), values are node specs. Opaque to Studio. The worker injects user parameters into this graph before submitting to ComfyUI.",
      "additionalProperties": true
    }
  }
}
